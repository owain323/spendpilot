# End-to-end web flow evidence (JUDGE-REPRODUCTION.md criteria 1-9)
# generated 2026-09-19T18:07:00Z by tools/e2e_flow.py — reproducible, isolated state
# approval model: authenticated web session mints a single-use HMAC mandate;
# the MCP surface refuses approval by design (see mcp-roundtrip.txt step 6)

# spawning backend on 127.0.0.1:60242 (isolated state)
## 0. authenticated session minted (workspace 9a7197354464)
## 1. agent speaks first (unprompted)
reply: I found 3 issue(s) worth your attention this month. Proven saving potential: $128.26/mo (scenario estimates).
cards: ['anomaly', 'anomaly', 'anomaly', 'kept']

## 2. prove the saving
before: 181.34 after: 96.2 label: scenario estimate from sample data — not a realized saving
proposal: p-ca1b8c35

## 3. approve -> signed mandate
reply: Approved. I issued signed mandate m-75d403e8 — single-use, scoped to ec2:ModifyInstanceAttribute with a hard cap of $181.34/mo, expiring at 18:22:01 UTC. Say "execute" and I will run it.

## 4. execute -> receipt
reply: Done. ec2:ModifyInstanceAttribute ran through the aws adapter (simulated), mandated by m-75d403e8. Scenario saving: $85.14/mo. The receipt is in the ledger.

## 5. execute again -> refused (brain level: no valid mandate remains)
reply: I hold no valid mandate, so I will not act. Ask me to "prove the saving", then "approve" — nothing executes on trust.

## 6. set budget -> warn
status: warn used_pct: 82.0

## 7. reopen session -> memory
reply: Welcome back. I remember our last conversation — your budgets are still on watch, and the ledger kept recording while you were away.

## 8. decision ledger
ledger count: 10
  execute - aws - Executed 'ec2:ModifyInstanceAttribute' via the aws adapter (simulated); mandate m-75d403e8
  approve - aws - web-session:9a7197354464 approved 'Rightsize the idle EC2 instance'; mandate m-75d403e8 sc
  propose - aws - Proposed 'Rightsize the idle EC2 instance' (47.0% scenario saving, proof attached) — await

## 9. Anthropic is KEEP, never anomaly
kept: ['Anthropic API']

E2E_FLOW_OK
